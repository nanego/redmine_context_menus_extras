<% if User.current.allowed_to?(:manage_issue_relations, @project) %>
  <li>
    <%= context_menu_link l(:contextual_menu_relates_to),
                          bulk_new_issue_relations_path(issue_id: @issue_ids.first, :ids => @issue_ids),
                          :remote => true,
                          :class => 'icon icon-attachment' %>
  </li>
<% end %>
